# 专线台账管理系统 v1.0 移植部署说明

## 1. 注意事项

本项目移植到其他 Windows 设备时，只同步代码，不同步业务数据库数据。

以下目录和文件不会进入 Git：

1. `data/`
2. `uploads/`
3. `node_modules/`
4. `dist/`

其中 `data/ledger.db` 是本机业务数据库，包含已导入的台账数据，不应上传 GitHub，也不应随代码包分发。

## 2. 新 Windows 设备准备

新设备需要安装：

1. Node.js 24 或更高版本。
2. Git。

安装后在项目目录执行：

```cmd
npm.cmd install
npm.cmd run db:init
start-dev.cmd
```

然后访问：

```text
http://127.0.0.1:5173
```

## 3. 数据迁移方式

如果新设备需要重新使用台账数据，推荐在新设备打开系统后重新导入 Excel 文件。

不建议直接复制 `data/ledger.db`，除非明确需要完整迁移本机数据库，并确认其中数据可以在目标设备保存。

## 4. v1.0 已包含功能

1. Excel 导入。
2. 台账列表。
3. 基础筛选和高级筛选。
4. 字段显示选择。
5. 单条编辑和批量编辑。
6. 月度统计。
7. 导出筛选结果。
8. 导入记录。
9. 修改记录。

