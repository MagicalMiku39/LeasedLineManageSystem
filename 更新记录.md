# 更新记录

## v1.4.2 - 2026-08-17

本版本补充自动化测试体系，并优化部分基础模块的可测试性和边界处理，降低后续功能迭代的回归风险。

### 工程质量

1. 新增前端工具函数测试，覆盖金额格式化、净增显示和筛选参数序列化。
2. 新增后端集成测试，覆盖账号认证、Excel 导入更新、台账查询与编辑、KPI 统计、客户经理统计和 Excel 导出等核心流程。
3. 新增 `npm test` 与 `npm run test:coverage` 命令，并设置代码覆盖率门槛。
4. 将通用前端函数拆分至独立模块，方便复用和测试。
5. 后端支持通过 `LEDGER_DB_PATH` 指定测试数据库，测试不会读写正式台账数据。
6. 优化月份参数校验、跨年月份计算及历史中文乱码文件名修复逻辑。

## v1.4.1 - 2026-08-14

本版本修正新增专线的统计口径，使新增数量以开始时间为主，并覆盖已经进入销户流程的当月新装专线。

### 统计规则调整

1. 月度、季度和年度新增均以“开始时间”是否落在对应统计周期内为准。
2. 新增状态调整为：`欠费销户`、`停机`、`停机执行中`、`销户(订单处理中)`、`正常在用`、`正式销户`。
   - 同时兼容 `销户（订单处理中）` 的全角括号写法。
3. 新增口径同步应用于首页新增统计、统计卡片点击结果，以及客户经理月度、季度、年度规模与收入净增。
4. 预计出账仍只按 `正常在用`、`停机执行中`、`停机` 三类可出账状态计算，不受新增状态扩展影响。
5. 销户仍以“状态时间”为准，停机仍以“状态时间”为准，原有口径不变。

## v1.4.0 - 2026-08-14

本版本新增全局“KPI统计模式”，统一控制首页台账、经营统计和客户经理统计的数据范围。

### 新增功能

1. 顶部栏新增“KPI统计模式”开关，默认开启。
2. 开启时仅统计并展示以下十类产品：
   - 互联网专线
   - 传输专线
   - OTN政企专网
   - 跨省互联网专线
   - 国际政企专网专线
   - 新跨省专线产品
   - SPN专线
   - MPLS VPN专线
   - 跨国专线A类产品
   - 跨省专线产品
3. KPI 产品范围统一应用于首页台账列表、导出、专线总数、正常在用、销户、月度新增/销户/停机、年度和季度新增/销户、预计总出账以及客户经理规模和收入净增统计。
4. 关闭开关后恢复统计和展示全部产品类型。

## v1.3.3 - 2026-08-09

本版本增强首页台账列表的排序能力，便于按任意展示字段快速整理当前筛选结果。

### 新增功能

1. 首页台账列表的每个展示字段均支持排序。
   - 点击表头首次按升序排列，再次点击切换为降序。
   - 当前排序字段显示方向标识，空值统一排在结果末尾。
2. 排序在服务端执行，适用于全部筛选结果和分页数据，不局限于当前页。
3. 导出 Excel 时沿用当前列表的筛选条件和排序顺序。

## v1.3.2 - 2026-08-09

本版本优化 Excel 更新导入时的资费数据保护规则，避免空白单元格误覆盖已维护的套餐资费。

### 修复

1. 导入已有专线记录时，若 Excel 的“活动套餐资费”为空，则保留系统内原有的“活动套餐资费”。
2. Excel 的“活动套餐资费”存在有效数值时，仍按导入值更新；新建专线记录仍按 Excel 原值写入。

## v1.3.1 - 2026-06-26

本版本修复客户经理页面在 Ubuntu 部署环境下接口异常时一直显示“加载中”的问题。

### 修复

1. 修复客户经理页面加载异常时无法退出“加载中”状态。
   - 客户经理统计接口请求失败、返回非 JSON 或返回错误状态时，前端会结束加载并显示错误提示。
   - 避免页面一直停留在“加载中”而看不到原因。
2. 优化客户经理统计接口错误返回。
   - 后端接口异常时返回 JSON 错误信息。
   - Ubuntu 部署时可直接在页面或服务日志中定位接口失败原因。

## v1.3 - 2026-06-26

本版本重点优化整体前端视觉，并新增客户经理经营数据展示页面，便于按客户经理查看规模和收入净增情况。

### 界面优化

1. 对系统前端进行整体视觉重构。
   - 新增 Apple 风格的浅色玻璃质感视觉层。
   - 优化顶部导航、登录页、统计卡片、筛选区、表格、详情抽屉和弹窗样式。
   - 保留原有首页结构和台账操作逻辑，不改变现有业务流程。
2. 修复右上角“导入 Excel”按钮与其他操作按钮不平行的问题。
   - 针对顶部操作区的上传按钮单独设置对齐样式。
   - 避免全局 `label` 样式影响上传按钮布局。

### 新增功能

1. 新增“客户经理”数据展示页面。
   - 顶部导航新增“客户经理”页签。
   - 支持勾选一个或多个客户经理。
   - 未勾选客户经理时，默认展示全部客户经理。
2. 新增客户经理净增统计。
   - 展示本月规模净增：新增条数 - 销户条数。
   - 展示本季度规模净增：新增条数 - 销户条数。
   - 展示本年度规模净增：新增条数 - 销户条数。
   - 展示本月收入净增：新增月资费合计 - 销户月资费合计。
   - 展示本季度收入净增：新增月资费合计 - 销户月资费合计。
   - 展示本年度收入净增：新增月资费合计 - 销户月资费合计。
3. 新增客户经理排名排序。
   - 支持按年度规模净增从高到低排序。
   - 支持按年度收入净增从高到低排序。
   - 支持按季度规模净增从高到低排序。
   - 支持按季度收入净增从高到低排序。
   - 支持按月度规模净增从高到低排序。
   - 支持按月度收入净增从高到低排序。

### 统计口径

1. 客户经理新增统计：
   - 状态包含：`正常在用`、`停机执行中`、`停机`。
   - 时间字段：`开始时间`。
2. 客户经理销户统计：
   - 状态包含：`欠费销户`、`正式销户`、`销户(订单处理中)`、`销户（订单处理中）`。
   - 时间字段：`状态时间`。
3. 客户经理收入净增：
   - 单条月资费优先使用 `实际月资费`。
   - 当 `实际月资费` 为空、为 0 或负数时，使用 `活动套餐资费`。
4. 月度、季度和年度范围：
   - 以页面选择的统计月份为基准。
   - 本季度为所选月份所在季度。
   - 本年度为所选月份所在年份。

### 工程调整

1. 新增客户经理统计接口 `/api/manager-performance`。
2. 将本地 Codex skill 安装产物加入 `.gitignore`：
   - `.agents/`
   - `skills-lock.json`

## v1.2 - 2026-06-18

本版本重点修复导入记录中文乱码，并将台账筛选升级为更接近 Excel 的多条件、多选值筛选。

### 修复

1. 修复导入记录中文乱码。
   - 浏览器上传 Excel 时，对 Multer 接收到的文件名进行 UTF-8 编码修复。
   - 导入记录接口兼容修复历史乱码文件名、工作表名和说明文本。
   - 导入明细接口兼容修复处理结果和说明文本乱码。

### 筛选增强

1. 归属区分支持多选。
2. 产品名称支持多选。
3. 产品状态支持多选。
4. 客户经理支持多选。
5. 同一字段选择多个值时使用 OR 关系，即匹配任意选中值。
6. 不同字段之间使用 AND 关系，即同时满足已设置的多个筛选条件。
7. 搜索、高级筛选、统计快捷筛选和多选筛选可以组合使用。
8. 导出功能继续沿用当前全部筛选条件，导出与列表结果保持一致。

### 首页统计增强

1. 首页左上角新增当前年份汇总。
   - 显示当前年份新增数量，使用绿色。
   - 显示当前年份销户数量，使用红色。
2. 首页左上角新增当前季度汇总。
   - 显示当前季度新增数量，使用绿色。
   - 显示当前季度销户数量，使用红色。
3. 年度和季度新增、销户统计沿用系统现有的新增/销户状态及时间口径。

## v1.1 - 2026-06-07

本版本在 v1.0 台账基础功能之上，重点增强了服务器部署、安全访问、出账统计口径和首页筛选交互。

### 新增功能

1. 增加 Ubuntu 服务器部署支持。
   - 后端可托管前端构建产物。
   - 生产环境可通过 `HOST`、`PORT` 等环境变量配置访问地址。
   - 新增 systemd 服务模板。
   - 新增 Ubuntu 部署说明。

2. 增加账号密码登录保护。
   - 首次打开系统需要初始化管理员账号。
   - 登录需要账号、密码和 Google Authenticator 6 位验证码。
   - 登录成功后 7 天内可自动登录。
   - 会话保存在服务端，浏览器使用 HttpOnly Cookie。

3. 增加 HTTPS 访问支持。
   - 支持 PEM 证书：`HTTPS_KEY_FILE`、`HTTPS_CERT_FILE`。
   - 支持 Windows PFX 证书：`HTTPS_PFX_FILE`、`HTTPS_PFX_PASSPHRASE`。
   - 新增 Windows 自签名证书生成脚本。
   - 新增 Windows HTTPS 启动脚本。
   - 新增 Ubuntu HTTPS 启动脚本。
   - `certs/` 已加入 `.gitignore`，证书和私钥不会上传 GitHub。

4. 首页统计卡片支持点击筛选。
   - 点击“专线总数”显示全部台账。
   - 点击“正常在用”筛选正常在用专线。
   - 点击“已销户”筛选销户类专线。
   - 点击“本月新增”按统计月份筛选新增专线。
   - 点击“本月销户”按统计月份筛选销户专线。
   - 点击“本月停机”按统计月份筛选停机专线。

### 统计规则调整

1. 月度新增统计：
   - 状态包含：`正常在用`、`停机执行中`、`停机`。
   - 时间字段：`开始时间`。

2. 月度销户统计：
   - 状态包含：`欠费销户`、`正式销户`、`销户(订单处理中)`、`销户（订单处理中）`。
   - 时间字段：`状态时间`。

3. 月度停机统计：
   - 状态包含：`停机`。
   - 时间字段：`状态时间`。

4. 预计总出账统计：
   - `预计总出账 = 预计月费出账 + 预计单次费用出账`。
   - 状态为 `正常在用` 时，统计月份正常出账。
   - 状态为 `停机执行中` 时，仅当 `状态时间` 在统计月份内出账。
   - 状态为 `停机` 时，仅当 `状态时间` 在统计月份内出账。
   - 其他状态不出账。
   - 当 `实际月资费 > 0` 时，优先使用实际月资费。
   - 当实际月资费为空、为 0 或负数时，使用活动套餐资费。
   - 单次出账费用仅在 `开始时间` 所在月份计入一次。

### 部署与安全说明

1. GitHub 不包含业务数据库数据。
2. 以下目录不会同步到 GitHub：
   - `data/`
   - `uploads/`
   - `node_modules/`
   - `dist/`
   - `certs/`
3. Ubuntu 首次部署后数据库为空，需要重新导入 Excel。
4. 管理员账号、密码哈希、Google Authenticator 密钥和登录会话保存在本地 `data/ledger.db` 中。

### 构建修复

1. 调整 Vite 配置，显式使用项目根目录，避免 Windows 中文路径和 OneDrive 路径下构建时出现 HTML 输出路径异常。
